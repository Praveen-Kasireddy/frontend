export enum ChartType {
    BAR = ('Bar' as any) as ChartType,
    COL = ('Col' as any) as ChartType,
    LINE = ('Line' as any) as ChartType,
    AREA = ('Area' as any) as ChartType,
    PIE = ('Pie' as any) as ChartType
}
