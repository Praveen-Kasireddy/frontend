export enum DragItemSourceType {
    Palette = ('Palette' as any) as DragItemSourceType,
    Group = ('Group' as any) as DragItemSourceType,
    Characteristic = ('Characteristic' as any) as DragItemSourceType,
    Filter = ('Filter' as any) as DragItemSourceType
}
