import { DebugElement } from '@angular/core';
import { NgxLoggerLevel } from 'ngx-logger';
// import { KosmosConfig } from '@core/models/configuration/KosmosConfig';
// export const environment: KosmosConfig = require(`json-loader!../assets/configs/config.json`);

export const environment = {
    production: true,
    traceRouting: false,
    configname: 'config.json'
};
